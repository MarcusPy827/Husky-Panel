# What is this? | 这是什么？
Some of the third party librar(ies) we depends does NOT have a `CMakeLists.txt` but only sources. To integrate those libraries, we wrote the missing `CMakeLists.txt`.

部分依赖的第三方库仅包含源码，不包含`CMakeLists.txt`。为了能正确集成这些库，我们编写了缺失的`CMakeLists.txt`。
